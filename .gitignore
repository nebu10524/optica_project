# Documentación local (PDF, patentes, pruebas, informes): no se sube al remoto.
Docs/

# Salidas locales de plantillas Word en la raíz (genéricas)
Documento_*.docx
Plantilla_*.docx

# Copias de entorno con secretos (nunca subir)
*.env.txt
backend/.env.txt

# Python
__pycache__/
*.py[cod]
