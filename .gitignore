# Documentación del proyecto (PDF, patentes, pruebas). Repo privado: se versiona.
# Si el repo pasara a ser público, valorar quitar Docs/ del historial o moverla a otro sitio.

# Salidas locales de plantillas Word en la raíz (genéricas)
Documento_*.docx
Plantilla_*.docx

# Copias de entorno con secretos (nunca subir)
*.env.txt
backend/.env.txt

# Python
__pycache__/
*.py[cod]
